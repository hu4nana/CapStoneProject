using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DashAttack_KnockBack : MonoBehaviour
{

    //private void OnCollisionEnter(Collision collision)
    //{
    //    Rigidbody col_Rigid = collision.gameObject.GetComponent<Rigidbody>();

    //    Vector3 direction = new Vector3((collision.transform.position.x -  transform.position.x),0,0).normalized;

    //    if (collision.gameObject.CompareTag("Player"))
    //    {
    //        //direction = (transform.position - collision.transform.position).normalized;
    //        //col_Rigid.AddForce(-direction * 100.0f, ForceMode.Impulse);
    //    }
    //}
}
